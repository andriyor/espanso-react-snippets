# espanso match file

# For a complete introduction, visit the official docs at: https://espanso.org/docs/

# You can use this file to define the base matches (aka snippets)
# that will be available in every application when using espanso.

# Matches are substitution rules: when you type the "trigger" string
# it gets replaced by the "replace" string.
matches:
  - trigger: ":rfce"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - /Users/andrejorehov/.nvm/versions/node/v14.17.5/bin/node
            - /Users/andrejorehov/WebstormProjects/espanso-react-snippets/src/snippets/rfce.js

  - trigger: ":rcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - /Users/andrejorehov/.nvm/versions/node/v14.17.5/bin/node
            - /Users/andrejorehov/WebstormProjects/espanso-react-snippets/src/snippets/rcc.js

  - trigger: ":rafc"
    replace: "{{output}}"
    vars:
        - name: output
          type: script
          params:
            args:
              - /Users/andrejorehov/.nvm/versions/node/v14.17.5/bin/node
              - /Users/andrejorehov/WebstormProjects/espanso-react-snippets/src/snippets/rafc.js


